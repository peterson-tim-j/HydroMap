The R package requires the non-R GUI application 'SAGA'. To install SAGA without the GUI, use the following stpw:

sudo apt-get install libwxbase3.0-dev
sudo apt-get install libgdal1-dev
./configure --disable-gui

